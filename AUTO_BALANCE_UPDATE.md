# ⚡ Автоматическое обновление баланса токенов

## ✅ Реализовано

Добавлено **динамическое обновление баланса токенов каждые 10 секунд** на следующих страницах:

1. **RecruiterDashboard** (`src/pages/RecruiterDashboard.tsx`)
2. **BuyTokens** (`src/pages/BuyTokens.tsx`)

---

## 🎯 Функциональность

### 1. Автоматическое обновление
- ✅ Баланс обновляется каждые **10 секунд** автоматически
- ✅ Запрос к API: `GET /api/v2/user/balance`
- ✅ Обновление происходит в фоне, не мешает пользователю

### 2. Визуальные индикаторы

#### RecruiterDashboard:
- 🎨 Анимация иконки монеты (поворот + увеличение)
- 🎨 Мигающая точка рядом с "Баланс токенов"
- 🎨 Плавное увеличение числа при обновлении

#### BuyTokens:
- 🎨 Мигающая точка рядом с "Текущий баланс"
- 🎨 Плавное увеличение числа при обновлении

### 3. Оптимизация
- ✅ Очистка интервала при размонтировании компонента
- ✅ Обработка ошибок (баланс остается прежним при ошибке)
- ✅ Проверка авторизации пользователя перед запросом

---

## 📝 Технические детали

### RecruiterDashboard.tsx

```typescript
// Новый state для индикатора обновления
const [balanceUpdating, setBalanceUpdating] = useState(false);

// Автоматическое обновление каждые 10 секунд
useEffect(() => {
  const loadBalance = async () => {
    try {
      setBalanceUpdating(true);
      const userId = getUserId();
      if (!userId) return;

      const balanceResponse = await apiGet<{ success: boolean; data: { tokens: number } }>('/api/v2/user/balance');
      if (balanceResponse.success && balanceResponse.data) {
        setTokenBalance(balanceResponse.data.tokens || 0);
      }
    } catch (error) {
      console.error('Error loading token balance:', error);
    } finally {
      setTimeout(() => setBalanceUpdating(false), 300);
    }
  };

  loadBalance();
  const intervalId = setInterval(loadBalance, 10000);
  
  return () => clearInterval(intervalId);
}, []);
```

### Визуальное отображение

```tsx
<Card className="bg-gradient-to-br from-forest-500 to-forest-600">
  <CardContent className="py-4 px-6">
    <div className="flex items-center gap-3">
      <Coins className={`w-8 h-8 text-white transition-transform duration-300 ${
        balanceUpdating ? 'scale-110 rotate-12' : 'scale-100'
      }`} />
      <div>
        <p className="text-xs text-forest-100">
          Баланс токенов
          {balanceUpdating && (
            <span className="ml-1 inline-block w-1 h-1 bg-forest-100 rounded-full animate-pulse"></span>
          )}
        </p>
        <p className={`text-2xl font-bold text-white transition-all duration-300 ${
          balanceUpdating ? 'scale-105' : 'scale-100'
        }`}>
          {new Intl.NumberFormat('ru-RU').format(tokenBalance)}
        </p>
      </div>
    </div>
  </CardContent>
</Card>
```

---

## 🎬 Как это работает

### Поток данных:

```
1. Компонент монтируется
   ↓
2. Загружается начальный баланс
   ↓
3. Устанавливается setInterval (10 сек)
   ↓
4. Каждые 10 секунд:
   - setBalanceUpdating(true)
   - API запрос к /api/v2/user/balance
   - Обновление state (tokenBalance / balance)
   - Анимация UI (300ms)
   - setBalanceUpdating(false)
   ↓
5. При размонтировании компонента:
   - clearInterval() очищает таймер
```

---

## 🧪 Тестирование

### Проверка работы:

1. **Откройте дашборд рекрутера** (`/recruiter`)
2. **Откройте DevTools** → Network → фильтр XHR
3. **Наблюдайте запросы** к `/api/v2/user/balance` каждые 10 секунд
4. **Смотрите анимацию** иконки монеты и числа баланса

### Проверка на странице покупки:

1. **Откройте страницу покупки токенов** (`/buy-tokens`)
2. **Следите за обновлением** баланса каждые 10 секунд
3. **Проверьте анимацию** числа баланса

---

## 🚀 Преимущества

### ✅ Для пользователя:
- Всегда актуальный баланс
- Мгновенное отображение после покупки/списания токенов
- Визуальная обратная связь (анимация)
- Не нужно перезагружать страницу

### ✅ Для системы:
- Легковесные запросы к API
- Автоматическая очистка ресурсов
- Обработка ошибок
- Не блокирует UI

---

## 📊 API Endpoint

```
GET /api/v2/user/balance
```

**Response:**
```json
{
  "success": true,
  "data": {
    "tokens": 20000
  }
}
```

---

## 🎨 CSS Анимации

### Используемые классы Tailwind:

```css
/* Анимация иконки */
transition-transform duration-300
scale-110 rotate-12

/* Анимация числа */
transition-all duration-300
scale-105

/* Мигающая точка */
w-1 h-1 bg-forest-100 rounded-full animate-pulse
```

---

## ⚙️ Настройки

### Изменение интервала обновления:

```typescript
// Текущее значение: 10 секунд (10000ms)
const intervalId = setInterval(loadBalance, 10000);

// Для изменения на 5 секунд:
const intervalId = setInterval(loadBalance, 5000);

// Для изменения на 30 секунд:
const intervalId = setInterval(loadBalance, 30000);
```

### Отключение анимации:

Удалите или закомментируйте классы:
```typescript
// Без анимации:
<Coins className="w-8 h-8 text-white" />

<p className="text-2xl font-bold text-white">
  {new Intl.NumberFormat('ru-RU').format(tokenBalance)}
</p>
```

---

## 🔧 Устранение неполадок

### Баланс не обновляется?

1. **Проверьте консоль** на наличие ошибок
2. **Убедитесь** что API endpoint доступен
3. **Проверьте авторизацию** пользователя
4. **Откройте Network** в DevTools и проверьте запросы

### Слишком частые обновления?

- Увеличьте интервал в `setInterval(loadBalance, 15000)` (15 сек)

### Анимация тормозит?

- Уменьшите продолжительность: `duration-150` вместо `duration-300`
- Или полностью отключите анимацию

---

## 📝 Changelog

**29.01.2026**
- ✅ Добавлено автообновление баланса в RecruiterDashboard
- ✅ Добавлено автообновление баланса в BuyTokens
- ✅ Реализованы визуальные индикаторы обновления
- ✅ Добавлена обработка ошибок
- ✅ Реализована автоматическая очистка ресурсов

---

## 🎯 Следующие шаги (опционально)

### Возможные улучшения:

1. **WebSocket** вместо polling для мгновенного обновления
2. **Показ истории транзакций** при клике на баланс
3. **Уведомления** при низком балансе
4. **Графики расхода** токенов за период
5. **Прогноз** когда закончатся токены

---

**Дата создания:** 29 января 2026  
**Версия:** 1.0  
**Статус:** ✅ Готово и протестировано
